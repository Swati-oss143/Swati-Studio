import {createClient} from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
const supabase=createClient('https://lmtqzkkyjebjdabahnnl.supabase.co','sb_publishable_RQOsIyFMOOegEknGAjNFsg_HVENkEEY');
const msg=document.getElementById('msg');
const form=document.querySelector('form');
form?.addEventListener('submit',async e=>{e.preventDefault();const btn=form.querySelector('button');btn.disabled=true;msg.textContent='Processing…';const fields=[...form.querySelectorAll('input')];const email=fields.find(x=>x.type==='email')?.value.trim();const password=fields.find(x=>x.type==='password')?.value;let result;if(form.id==='registerForm'){const name=document.getElementById('name').value.trim();result=await supabase.auth.signUp({email,password,options:{data:{display_name:name}}});}else{result=await supabase.auth.signInWithPassword({email,password});}btn.disabled=false;if(result.error){msg.textContent=result.error.message;return;}msg.textContent='Success. Redirecting…';location.href=form.id==='adminLoginForm'?'index.html':'../dashboard.html';});
