# User/Admin flow fix

User-facing pages use only the user workspace sidebar and user routes.
Admin pages remain separate and are not linked from user navigation.

Changed pages: dashboard navigation peers: projects, tools, wallet, transactions, notifications, support, profile.
